/** Shared with middleware and server-side API client — keep in sync with backend SessionMiddleware. */
export const ANON_SESSION_COOKIE = "trestle_anon_session";
export const ANON_SESSION_MAX_AGE_SECONDS = 60 * 60 * 24 * 30;
